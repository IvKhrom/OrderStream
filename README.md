# OrderStream — микросервис для управления заказами

Небольшой учебный проект: HTTP API + Worker. Проект демонстрирует архитектуру event-driven обработки заказов с использованием PostgreSQL (партицирование), Kafka (events + ACK) и Redis (кеш). Реализовано ожидание ACK в API: запрос клиента завершится только после того, как воркер обработает событие и отправит ACK.

## Ключевые компоненты
- **API**: HTTP-интерфейс для CRUD-подобных операций над заказами (`cmd/api`). Отвечает за валидацию, запись в БД, публикацию событий в Kafka и ожидание ACK от воркера.
- **Worker**: консюмер событий (`cmd/worker`). Получает события из Kafka, обрабатывает заказ (бизнес-логика), обновляет статус в Postgres и публикует ACK в Kafka.
- **Postgres**: хранение заказов. Таблица `orders` партиционирована (4 партиции по bucket).
- **Kafka**: топики `orders.events` (события create/update/delete) и `orders.ack` (подтверждения обработки).
- **Redis**: опциональный кеш (клиент `internal/adapters/redis`).

## Архитектурный поток
1. Клиент -> `POST /orders` (API) — создаёт запись в Postgres со статусом `new`.
2. API публикует событие в Kafka (`orders.events`).
3. Worker считывает событие, выполняет обработку, обновляет запись в Postgres (например, `processing` → `done`).
4. Worker публикует ACK в топик `orders.ack`.
5. API слушает `orders.ack` и, при получении ACK для конкретного `order_id`, возвращает ответ клиенту.

## Структура репозитория
- `cmd/api` — entrypoint для API.
- `cmd/worker` — entrypoint для воркера.
- `internal/adapters/*` — адаптеры к Kafka/Postgres/Redis.
- `internal/api` — HTTP router и handlers.
- `internal/domain` — доменные структуры (Order, события).
- `internal/repository` — интерфейсы репозиториев.
- `internal/mocks` — ручные моки для тестов (и место для сгенерированных mockery-моков).
- `migrations` — SQL миграции.

---

## Быстрый старт (Docker)

Поднять все сервисы локально (рекомендуемый способ для тестирования):

```powershell
docker compose up -d --build
```

Это поднимет (в зависимости от `docker-compose.yml`): Zookeeper, Kafka, Postgres, Redis, а также сервисы `api` и `worker` (если они включены). API доступен на `http://localhost:8080`.

### Запуск сервисов локально (с контейнерной infra)
Для отладки кода вы можете запускать `api` и `worker` локально, используя контейнеры для инфраструктуры (Kafka/Postgres/Redis):

```powershell
# Остановите контейнерный экземпляр API, чтобы освободить порт
docker compose stop api

# Запуск API локально
cd .\cmd\api
go run .\main.go

# В отдельном терминале — воркер
cd ..\worker
go run .\main.go
```

---

## Миграции (Postgres)

Миграции хранятся в папке `migrations/`:
- `0001_init.up.sql` — инициализация таблицы `orders` и партиций.
- `0002_remove_external_id.up.sql` — удаление колонки `external_id`.


> В текущей версии приложения внешний marketplace id хранится внутри JSON-поля `payload` (в `payload->>'id'`).

Пример применения миграции вручную (PowerShell):

```powershell
docker cp .\migrations\0002_remove_external_id.up.sql orderstream-postgres-1:/tmp/0002_remove_external_id.up.sql
docker exec -it orderstream-postgres-1 psql -U postgres -d orderstream -f /tmp/0002_remove_external_id.up.sql
```


---

## HTTP API — все ручки

- `GET /health`
  - Возвращает `ok`.

- `POST /orders` — создание / обновление / soft-delete
  - Создание (новый заказ): `order_id` = `"0"` или пустая строка -> API сгенерирует внутренний `order_id`.
  - Обновление: передаёте внутренний `order_id` (UUID) — обновляется `payload`.
  - Удаление (soft-delete): укажите `"status": "deleted"` с `order_id` и `user_id`.

  Примеры запросов:

  Создание:
  ```json
  {
    "order_id": "0",
    "user_id": "<uuid>",
    "payload": { "id": "ext-123", "items": [...] }
  }
  ```

  Обновление:
  ```json
  {
    "order_id": "<our-uuid>",
    "user_id": "<uuid>",
    "payload": { ... }
  }
  ```

  Soft-delete:
  ```json
  {
    "order_id": "<our-uuid>",
    "user_id": "<uuid>",
    "status": "deleted"
  }
  ```

  Ответы:
  - 201 Created: `{ "order_id": "<uuid>", "status": "created" }` при создании
  - 200 OK: `{ "order_id": "<uuid>", "status": "updated" }` при успешном обновлении
  - 200 OK: `{ "status": "deleted" }` при soft-delete
  - 409 Conflict: `{ "status": "deleted" }` если попытка обновления удалённого заказа

- `GET /orders/{id}`
  - Получение заказа по внутреннему `order_id` (UUID).

- `GET /orders/by-external/{external}?user_id=<uuid>`
  - Поиск по внешнему marketplace id — значение берётся из `payload->>'id'`.

---

## Тесты и mockery

В проекте уже есть unit-тесты, расположенные рядом с исходниками (`internal/*`). Для моков есть два варианта:

1. Ручные моки: `internal/mocks` — позволяют запускать тесты без генерации.
2. Генерация mockery: `go:generate` директивы добавлены в интерфейсные файлы. Чтобы сгенерировать моки локально:

```powershell
go install github.com/vektra/mockery/v2@latest
go generate ./...
```

После этого mockery создаст / обновит моки в `internal/mocks`.

### Запуск тестов

```powershell
go test ./... -v

# Получить покрытие по пакетам
go test ./... -cover

# Сгенерировать единый профиль покрытия
go test ./... -coverpkg=./... -coverprofile=coverage.out
go tool cover -func=coverage.out
```

Обратите внимание: в некоторых окружениях (PowerShell, пути с кириллицей) генерация `coverage.out` и запуск `go tool cover` могут требовать небольших корректировок — см. локальные ошибки в терминале, если они появятся.

---

## Рекомендации по улучшению покрытия и CI

- Для 100% покрытия нужно покрыть все ветки ошибок и успешных сценариев в адаптерах и API. Для адаптеров, зависящих от внешних сервисов (Postgres, Redis, Kafka), используйте моки или интеграционные тесты с контейнерами.
- Рекомендую добавить GitHub Actions workflow, который выполняет `go generate ./...`, затем `go test ./... -coverprofile=coverage.out -coverpkg=./...` и публикует артефакт `coverage.out`.

---


# OrderStream - Микросервис для управления заказами


### 1. Ускоренное вычисление bucket
- Используется простой остаток от деления вместо CRC32
- Быстрее вычисление партиции для заказа

### 2. Синхронная обработка с ожиданием ACK
- API ждёт подтверждения обработки от Worker
- Клиент получает ответ только после успешной обработки
## 🚀 Быстрый старт

1. Запуск инфраструктуры (Docker Compose)

```powershell
docker compose up -d --build
```

Это поднимет Zookeeper, Kafka, Postgres, Redis, а также сервисы `api` и `worker`, если они указаны в `docker-compose.yml`.

API будет доступен по `http://localhost:8080`.

2. Запуск сервисов локально (для отладки логов)

Если вы хотите запускать `api` и `worker` локально (используя контейнерную инфраструктуру для Kafka/Postgres/Redis), остановите только API-контейнер и затем запустите бинарники локально:

```powershell
# Остановить API-контейнер (если он запущен)
docker compose stop api

# В одном окне терминала — API
cd .\cmd\api

go run .\main.go

# В другом окне терминала — Worker
cd ..\worker
go run .\main.go
```

3. Применение миграций

Миграции находятся в папке `migrations`:

- `0001_init.up.sql` — инициализация таблицы `orders` и её партиций
- `0002_remove_external_id.up.sql` — удаление устаревшего столбца `external_id`
- `0002_remove_external_id.down.sql` — откат изменения (восстановит колонку `external_id` и индекс)

Применить миграции вручную можно через `psql` внутри контейнера Postgres. Пример применения конкретного файла:

```powershell
# Скопировать файл в контейнер
docker cp .\migrations\0002_remove_external_id.up.sql orderstream-postgres-1:/tmp/0002_remove_external_id.up.sql

# Применить миграцию к базе `orderstream`
docker exec -it orderstream-postgres-1 psql -U postgres -d orderstream -f /tmp/0002_remove_external_id.up.sql
```

Откат (восстановление `external_id`):

```powershell
docker cp .\migrations\0002_remove_external_id.down.sql orderstream-postgres-1:/tmp/0002_remove_external_id.down.sql
docker exec -it orderstream-postgres-1 psql -U postgres -d orderstream -f /tmp/0002_remove_external_id.down.sql
```

Важно: перед применением миграций в production сделайте бэкап БД.

4. Обновлённая логика идентификаторов (важно)

- `order_id` — внутренний UUID, единственный идентификатор заказа внутри системы.
- `payload.id` — внешний (marketplace) id. По нему выполняется поиск через `GET /orders/by-external/{marketplace_id}`.
- Поле `external_id` было удалено из схемы (миграция `0002_remove_external_id.up.sql`).

5. API Endpoints (текущая реализация)

- GET /health
  - Проверка статуса, отвечает `ok`.

- POST /orders
  - Создание или обновление заказа.
  - Request JSON:

    - Create (новый заказ):
      ```json
      {
        "order_id": "0",          // или пустая строка -> будет создан новый order_id
        "user_id": "<uuid>",
        "payload": { "id": "565", "name": "Product A", ... }
      }
      ```

    - Update (обновление существующего заказа):
      ```json
      {
        "order_id": "<our-uuid>",
        "user_id": "<uuid>",
        "payload": { ... обновлённый payload ... }
      }
      ```

  - Responses:
    - 201 Created: `{ "order_id": "<uuid>", "status": "created" }` при создании
    - 200 OK: `{ "order_id": "<uuid>", "status": "updated" }` при обновлении
    - 409 Conflict: `{ "status": "deleted" }` если заказ помечен как `deleted` и не может быть обновлён

- GET /orders/{id}
  - Поиск по внутреннему `order_id` (UUID). Возвращает объект заказа.

- GET /orders/by-external/{external}?user_id=<uuid>
  - Поиск по внешнему marketplace id — ищется значение `payload->>'id'`.
  - Обязателен query-параметр `user_id`.

6. Удаление заказа и кеша

Удаление в системе реализовано как soft-delete: запись в базе остаётся, но помечается со статусом `deleted` и дальнейшие изменения запрещены.

Как пометить заказ удалённым (через API — отправьте обновление с `status: "deleted"`):

```json
POST /orders
Content-Type: application/json

{
  "order_id": "<our-order-uuid>",
  "user_id": "<user-uuid>",
  "status": "deleted"
}
```

Поведение после такой операции:
- API вызывает `repo.DeleteOrder` — это soft-delete (в базе `status='deleted'`).
- Ответ на операцию удаления: `200 OK` с телом `{"status":"deleted"}` (без полной информации о заказе).
- Любые последующие попытки обновления этого заказа вернут `409 Conflict` с тем же телом `{"status":"deleted"}` — т.е. заказ остаётся в БД, но изменить его нельзя.

Если нужно физически удалить запись (например, для тестов), используйте SQL напрямую (ОСТОРОЖНО):

```powershell
docker exec -it orderstream-postgres-1 psql -U postgres -d orderstream -c "DELETE FROM orders WHERE order_id='<order_id>'"
```

Удаление ключей в Redis (если используются ключи с `order_id`):

```powershell
docker exec orderstream-redis-1 redis-cli --scan --pattern "*<order_id>*" | xargs -r -n1 redis-cli DEL
```



- API сохраняет заказ в PostgreSQL с статусом "new"

- API отправляет событие в Kafka и ждёт ACK

- Worker получает событие, обрабатывает заказ

- Worker обновляет статус в PostgreSQL ("processing" → "done")

- Worker отправляет ACK в Kafka

- API получает ACK и отвечает клиенту